import React from "react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Link2Off, Users, Wallet, Globe, AlertCircle } from "lucide-react";

const scams = [
  {
    icon: <Link2Off className="h-10 w-10 text-crypto-red" />,
    title: "Phishing Attacks",
    description: "Fake websites and emails that mimic legitimate crypto services to steal your private keys and passwords."
  },
  {
    icon: <Users className="h-10 w-10 text-crypto-red" />,
    title: "Ponzi & Pyramid Schemes",
    description: "Projects promising unrealistic returns, relying on new investor funds to pay earlier investors."
  },
  {
    icon: <Wallet className="h-10 w-10 text-crypto-red" />,
    title: "Fake ICOs & Token Sales",
    description: "Fraudulent initial coin offerings with no real product or intention to deliver on promises."
  },
  {
    icon: <Globe className="h-10 w-10 text-crypto-red" />,
    title: "Fake Exchanges",
    description: "Counterfeit trading platforms designed to steal your deposits with no way to withdraw funds."
  },
  {
    icon: <AlertCircle className="h-10 w-10 text-crypto-red" />,
    title: "Pump & Dump Schemes",
    description: "Artificially inflating coin prices through false recommendations, then selling at a profit."
  }
];

const ScamTypes = () => {
  return (
    <section id="scam-types" className="py-16 md:py-24 bg-white">
      <div className="container px-4 md:px-6">
        <div className="flex flex-col items-center text-center space-y-4">
          <h2 className="text-3xl font-bold tracking-tighter sm:text-4xl md:text-5xl">
            Common Cryptocurrency Scams
          </h2>
          <p className="max-w-[700px] text-gray-500 md:text-xl">
            Fraudsters are constantly evolving their tactics. Here are the most prevalent crypto scams to watch out for.
          </p>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-12">
          {scams.map((scam, index) => (
            <Card key={index} className="animate-fade-in" style={{ animationDelay: ${index * 100}ms }}>
              <CardHeader className="flex flex-row items-center gap-4 pb-2">
                {scam.icon}
                <CardTitle className="text-xl">{scam.title}</CardTitle>
              </CardHeader>
              <CardContent>
                <CardDescription className="text-base">{scam.description}</CardDescription>
              </CardContent>
            </Card>
          ))}
        </div>
      </div>
    </section>
  );
};

